{
  "name": "vvidhya.com",
  "version": "0.0.0",
  "private": true,
  "main": "functions/api/submit-contact-form.js",
  "scripts": {
    "build": "esbuild functions/api/submit-contact-form.js --bundle --outfile=dist/worker.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "tailwindcss": "^3.4.3",
    "vitest": "2.0.5",
    "wrangler": "^3.92.0"
  },
  "dependencies": {
    "esbuild": "^0.24.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "resend": "^4.0.1"
  }
}
